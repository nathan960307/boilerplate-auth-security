jwt:
  secret: ${JWT_SECRET}
  access-expiration: ${JWT_ACCESS_EXPIRATION}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boilerplate?serverTimezone=Asia/Seoul
    username: bm
    password: 123qwe!@#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 처음엔 create 또는 update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
